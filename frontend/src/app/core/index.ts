// Core services + cross-cutting concerns (auth, http interceptor, ws, models).
// Phase 1 will populate auth.service.ts, http.interceptor.ts, api.service.ts,
// auth.guard.ts, ws.service.ts, models.ts.
export {};
