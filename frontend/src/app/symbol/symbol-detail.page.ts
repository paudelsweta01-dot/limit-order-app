import { Component } from '@angular/core';

/** Placeholder until Phase 5. Architecture §6.3. */
@Component({
  selector: 'app-symbol-detail',
  template: `<h2>Symbol detail</h2><p>Phase 5 lands the order book + place-order form.</p>`,
})
export class SymbolDetailPage {}
